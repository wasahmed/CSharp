using System;

namespace numbers
{
    class numbers
    {
        static void Main(string[] args)
        {
            //some Math methods
            Console.WriteLine(Math.Abs(-10));
            Console.WriteLine(Math.Pow(2, 2));
            Console.WriteLine(Math.Max(4, 10));
            Console.WriteLine(Math.Round(4.6));
        }
    }
}
